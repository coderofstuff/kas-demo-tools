{
  "name": "kas-demo-tools",
  "version": "0.0.1",
  "description": "A bunch of random scripts to demonstrate some functionality of the WASM SDK.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "coderofstuff",
  "license": "MIT",
  "dependencies": {
    "websocket": "^1.0.35"
  }
}
